

def generate_markdown(root_dir: str, output_dir: str):
    """
    Crawls a directory structure and generates HTML files from Markdown files it finds.

    Args:
        root_dir (str): The root directory to crawl.
        output_dir (str): The directory where the HTML files will be saved.
    """
    pass
